[alias]
	br = branch
  co = checkout
  db = branch -d 
  logg   = log --color --graph --abbrev-commit --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'

[user]
	email = gahoward@amgen
	user = Traap
	name = gahoward

[core]
	excludesfile = ~/.gitignore_global
	editor = /usr/local/bin/vim

[push]
	default = simple

[color]
	ui = always

[code]
	pager = cat

[url "https://git-codecommit.us-east-1.amazonaws.com/v1/repos"]
	insteadof = mhp:

[credential "mhp:"]
	UseHttpPath = true
	helper = !aws --profile CodeCommitProfile codecommit credential-helper $@
	username = ghoward

[credential "https://gitlab-dal.devops.amgen.com"]
  username = gahoward
	helper = store

[credential "https://github.com"]
  username = Traap 
	helper = store

